services:
  web:
    build: .
    container_name: django_app
    working_dir: /app/To_Do_List_Projects
    command: python manage.py runserver 0.0.0.0:8000

    volumes:
      - .:/app

    ports:
      - "8000:8000"

    environment:
      - PYTHONUNBUFFERED=1